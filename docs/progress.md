# TRACE Proof progress log

## 2026-09-16 — Stage 0: workspace scaffold

- **Elapsed:** Approximately 15 minutes; started with 10 hours remaining.
- **Changed:** Created independent React/Vite and Hardhat 3 ethers/TypeScript packages, runtime pin, ignore rules, environment templates, and root project documentation.
- **Validation:** Node 22.19.0 satisfies the current documented Vite and Hardhat minimums. `frontend`: production build and lint passed; the development server returned HTTP 200. `contracts`: Solidity 0.8.34 compiled, three Solidity tests and two TypeScript tests passed, and TypeScript typechecking passed. A public Sepolia RPC returned chain ID 11155111. Both populated `.env` paths are ignored, `.env.example` files remain trackable, no files are staged, and the repository scan found no private-key-shaped value or mnemonic assignment outside ignored dependencies and lockfiles.
- **Known limitations:** Official event rules and rubric were not supplied. Testnet wallet ownership and balances cannot be verified without the two public account addresses; no faucet request was made, although Ethereum's current network documentation lists multiple Sepolia faucets. The frontend dependency audit reports zero vulnerabilities. The official Hardhat ethers scaffold currently reports 14 development-tooling advisories (12 low, 1 moderate, 1 high); production-only audit reports zero, and the suggested complete fix would introduce breaking package versions, so no forced audit rewrite was applied. No TRACE Proof application logic or deployment exists yet.
- **AI assistance:** AI was used to inspect the blueprint, verify official tooling requirements, scaffold both packages, prepare configuration/documentation, and run validation. The developer must review the generated files and recorded results before committing.

## 2026-09-16 — Stage 1: reproducible demo fixture

- **Elapsed:** Approximately 25 minutes; started with about 9 hours 45 minutes remaining.
- **Changed:** Added canonical ASCII ID normalization and bytes32 conversion, string-based INR-to-paise parsing and formatting, browser-compatible SHA-256 helpers, typed campaign/donation/allocation/expense fixtures, derived metric calculations, five fictional SVG receipt fixtures, and a compact fixture summary screen. Added ethers v6 and Vitest with exact lockfile updates.
- **Validation:** Five test files and 31 tests pass. The fixture reconciles ₹10,000 recorded and allocated, ₹7,700 in expense claims, ₹2,300 unclaimed, ₹4,800 attested, ₹900 flagged, and ₹2,000 awaiting review. Coverage metrics are 77.0%, 74.0%, and 62.3%. Tests cover `0.01`, `10.10`, a large value, negatives, empty input, exponent/comma syntax, zero, excessive decimals, canonical ID round-trips, the 31-byte limit, parent relationships, allocation capacity, zero-denominator percentages, receipt size limits, all frozen SHA-256 values, and the original/altered mismatch. Production build and lint pass.
- **Known limitations:** Data is a development fixture and is not yet recorded on-chain. Receipt SVGs are fictional static assets; a digest match proves byte equality only. The current screen is a fixture preview, not the Stage 4 chain-backed public experience. The live ₹1,200 sample receipt is prepared but deliberately excluded from initial ₹7,700 metrics. No contract changes or deployment were made.
- **AI assistance:** AI generated the Stage 1 modules, fictional receipt artwork, test cases, fixture preview, and documentation, then ran the reported tests, build, and lint. The developer must review the files and results before committing.

## 2026-09-16 — Stage 2: tested ProofRegistry

- **Elapsed:** Approximately 25 minutes; started with about 9 hours 20 minutes remaining.
- **Changed:** Replaced the sample Counter with a single nonpayable ProofRegistry contract. Added immutable campaign/NGO/reviewer identities, donation/allocation/expense records, constant-time parent capacity counters, global totals and record counts, role-gated mutations, explicit record getters, custom errors, reconstructable events, latest reviewer state, and append-only review history. Replaced the sample tests and documentation with the TRACE Proof contract suite.
- **Validation:** Solidity 0.8.34 compilation passed. TypeScript typechecking against the generated ABI passed. All 23 Hardhat ethers/Mocha tests passed, covering constructor roles, the complete event-backed happy path, unauthorized callers, duplicate and empty IDs, zero amounts, missing parents, empty receipt digests, exact allocation/expense limits, one-paise overflow, unchanged counters after reverts, missing expenses, invalid review state/reason, duplicate decisions, corrected re-reviews with preserved history, missing-record getters, and flagged claims retaining allocation consumption.
- **Known limitations:** The contract has not been deployed or seeded on a public testnet. Roles are intentionally immutable for this prototype. Claims cannot be edited, deleted, refunded, or reconciled with real payments. On-chain rules constrain declared values and attribution; they cannot prove that a receipt is authentic, that an NGO disclosed every expense, or that separate addresses represent independent people. Event-based discovery will be implemented in the read repository during later stages.
- **AI assistance:** AI used the generated Hardhat and ethers project guidance to implement the contract, write the TypeScript suite, interpret failures, and run compilation, typechecking, and tests. The developer must review the code and reported results before committing.

## 2026-09-17 — Stage 3: public deployment and canonical seed complete

- **Elapsed:** Completed across the secure account-funding and live-deployment checkpoint that began with about 7 hours 55 minutes remaining.
- **Changed:** Added separate read/write Sepolia network configurations, two-account readiness checks, exact-byte canonical seed data, guarded deployment metadata, confirmed and resumable seed writes, provider-compatible bounded event queries, public-state verification, frontend ABI/deployment export, a local end-to-end rehearsal, encrypted-keystore instructions, and `.env` loading. Added focused chunk-boundary tests. Deployment files are written atomically and contain only public addresses, blocks, transaction hashes, and explorer links.
- **Deployment:** ProofRegistry is live on Ethereum Sepolia at [`0x8b123800F17CbeBCE3546678A70a0b5414b779e3`](https://sepolia.etherscan.io/address/0x8b123800F17CbeBCE3546678A70a0b5414b779e3), deployed in block `11718513` by transaction [`0x5fd30c59e1f23737dff9b6b8eda870691a63c0c219955b4419d81f047f435361`](https://sepolia.etherscan.io/tx/0x5fd30c59e1f23737dff9b6b8eda870691a63c0c219955b4419d81f047f435361). Nine confirmed seed transactions recorded 1 donation, 3 allocations, 3 expenses, and 2 reviews; all references are preserved in `contracts/deployments/sepolia.json`.
- **Validation:** Account readiness passed with distinct funded NGO and reviewer addresses. Solidity compilation and TypeScript checking pass. All 26 Hardhat tests pass, including the original 23 contract boundaries and 3 bounded-query tests. The local rehearsal passes. The live verifier confirms ₹10,000 recorded, ₹10,000 allocated, ₹7,700 claimed, exact receipt digests, Food attested, Medical unreviewed, Logistics flagged, and event counts of 1/3/3/2. Independent public RPC reads returned the same counters, totals, review states, and nine logs. Frontend export, 31 tests, production build, and lint pass.
- **Known limitations:** The contract is not source-code verified on Etherscan; the explorer still exposes the address, bytecode, transactions, and events. Prototype roles are immutable. Testnet ETH has no monetary value. On-chain data preserves declared claims and reviewer decisions but cannot establish receipt authenticity, payment occurrence, completeness, legal identity, or reviewer independence. The current frontend remains the Stage 1 fixture preview; chain-backed reads and wallet write flows belong to later stages.
- **AI assistance:** AI implemented and tested the deployment pipeline, diagnosed private-key-format and RPC log-range failures without reading secrets, independently checked public chain state, and generated the public configuration. The developer supplied demo-only credentials through Hardhat's encrypted keystore and approved the live transactions. Review the code and transaction record before committing.

## 2026-09-17 — Stage 4: wallet-free public chain reads

- **Changed:** Replaced the fixture-only screen with a typed chain repository, adaptive bounded event reads, deterministic parent-child reconstruction, a single-block live source marker, campaign metrics, needs-attention rows, canonical donation search, allocation/expense trails, receipt digests, latest review states, append-only review history, explorer references, and a nine-event timeline. Added explicit loading, wrong/unavailable RPC, unknown-record, and user-selected cached-snapshot states. Added responsive campaign/trace pages, reusable source/search/metadata components, hash routes, display helpers, and frontend setup documentation.
- **Validation:** A fresh in-app browser with no wallet loaded the live Sepolia deployment at block `11720633`, displaying 1 donation, 3 allocations, 3 expenses, 2 reviews, the expected ₹10,000/₹7,700 metrics, exact roles/digests, and nine real transaction links. `DON-NOT-FOUND` produced “No record found” after a successful chain read. A separate build with an intentionally unreachable RPC produced “Unable to check Sepolia”; no fixture appeared until “View labeled cached snapshot” was selected, after which the source badge clearly showed block `11718560` and “not freshly checked.” Eight test files and 39 tests pass, including event reconstruction, parent integrity, re-review ordering, adaptive RPC ranges, routes, and snapshot reconciliation. Production build and lint pass with no warnings; the ethers repository is loaded as a separate lazy chunk.
- **Known limitations:** This stage is read-only. Receipt files are not compared yet, arbitrary receipt bytes are not hosted, and NGO/reviewer wallet writes are not exposed. The bundled snapshot is a recovery view rather than fresh chain evidence. Public RPC availability and browser CORS remain external dependencies. Event reads currently scan forward from the deployment block on each refresh; the small prototype does not use an indexer or persistent block cache.
- **AI assistance:** AI implemented and tested the chain-read architecture and public screens, exercised live Sepolia and controlled failure paths in a fresh browser, and updated documentation. Review the source and visible claims before creating the Stage 4 commit.

## 2026-09-17 — Stage 5: local receipt integrity comparison

- **Changed:** Added one local integrity panel per expense, fictional sample receipt downloads, a 5 MB file boundary, race-safe browser SHA-256 calculation, normalized digest comparison, and explicit idle/hashing/matched/mismatched/error states. The three-part evidence summary now updates file integrity independently from claim-record and reviewer-decision status. Match and mismatch explanations preserve the blueprint’s truth boundary, and selected SVG/PDF/image bytes are neither uploaded nor injected into the page.
- **Validation:** Nine test files and 44 tests pass. Tests reproduce every frozen receipt digest, confirm the original/altered food receipts differ, accept the exact configured size boundary, reject empty/oversized files, surface file-read failures, verify sample-to-expense routing, and assert that local verification makes no `fetch` request. Production build and lint pass. In a fresh wallet-free browser backed by the live Sepolia claim, selecting `food-supplies-original.svg` produced “Hash matched” with digest `0x24a512…e4c431`; replacing it with `food-supplies-altered.svg` produced “Hash mismatch” with digest `0x6afc60…9e16d`. The separate Food review remained Attested in both cases.
- **Known limitations:** Digest equality proves only that selected bytes match the recorded fingerprint. It does not establish receipt truth, payment, vendor identity, or complete disclosure. Synthetic samples are publicly hosted, but arbitrary new receipts remain local to the submitting user and are not retrievable on another device. Browser Web Crypto requires a secure context such as HTTPS or localhost. Wallet writes remain outside this stage.
- **AI assistance:** AI implemented and tested the local comparison component, exercised original and visibly altered receipt paths in the browser, inspected the result visually, and updated scope documentation. Review the diff before creating the Stage 5 commit.

## 2026-09-17 — Stage 6: authorized NGO expense submission

- **Changed:** Added the `#/submit` NGO workspace, injected-wallet discovery and explicit connection, Sepolia/account authorization, account and chain-change invalidation, remaining-capacity display, integer-paise validation, local 5 MB receipt hashing, canonical unique expense IDs, optional clearly local description, final contract-input summary, and an on-demand loaded ethers signer. Added duplicate preflight checks, a single-flight click guard, awaiting-wallet/pending/confirmed/rejected/failed/uncertain states, post-receipt record refresh, transaction and trail links, and uncertain-receipt recovery by checking the expense record before retrying. Cached snapshot mode cannot write, and the frontend contains no private signing material.
- **Validation:** Eleven test files and 70 tests pass. New tests cover wallet absence, explicit connection, the correct NGO, an unauthorized account, wrong network, final pre-write authorization, account/network listeners, exact ₹1,200 capacity, one-paise overflow, invalid INR forms, duplicate local and on-chain IDs, click suppression, wallet rejection, wallet failure, mined failure, uncertain confirmation, recovered confirmation, successful receipt lifecycle, and refresh callback. Existing receipt tests cover empty, exact-limit, oversized, raw-byte hashing, and file-read failure. Production TypeScript/Vite build and lint pass. The operator approved the live claim for `EX-MU4XGHLR-1YS0HMT`; [transaction `0x1b642d…01c6e`](https://sepolia.etherscan.io/tx/0x1b642de0370fa773dc7f8ad67e49a80868612b4e853dcc8cce1c225bae601c6e) confirmed in block `11721074`. Independent getters/events verify the configured NGO signer, ₹1,200 amount, exact `0x3163…697c` receipt digest, four expenses, ₹8,900 total claims, ₹6,000 Food claims, and zero Food remainder after refresh.
- **Known limitations:** Arbitrary receipt bytes remain local and cannot be retrieved by a reviewer on another device. A digest match does not establish truth or payment. Public RPC and injected-wallet availability are external dependencies, and the prototype uses mined-receipt confirmation rather than a stronger production finality policy.
- **AI assistance:** AI implemented the wallet adapter, submission workflow, UI, recovery logic, tests, and documentation, and performed non-writing browser QA. The developer must inspect the prepared claim and explicitly approve any live testnet transaction.

## 2026-09-17 — Stage 7: attributed reviewer decisions

- **Changed:** Added the `#/review` workspace with an unreviewed/flagged queue, complete selected-claim details, explorer references, independent local receipt comparison, latest decision, and append-only history. Generalized injected-wallet state for separate immutable NGO and reviewer roles, added reviewer-only preflight immediately before signing, decision/reason enum validation, identical-latest-review protection, lazy-loaded contract writing, single-flight submission, complete transaction states, uncertain-receipt recovery against current contract state, and post-confirmation refresh. The UI preserves the selected expense, decision, and reason after rejection or failure and disables all writes for cached data, wrong chain, NGO, or any unauthorized account.
- **Validation:** Twelve test files and 80 tests pass. Stage 7 tests cover the separate reviewer and NGO roles, unavailable/unauthorized/wrong-network wallet boundaries, account and chain listeners, every supported decision/reason mapping, invalid inputs, identical repeat prevention, corrected re-review, missing/uncertain preflight, duplicate clicks, wallet rejection, wallet failure, mined failure, successful confirmation and refresh, and uncertain confirmation recovery. Existing reconstruction tests verify latest-state replacement with preserved prior review history. The operator separately approved the new claim's Attested / Document reviewed decision; [transaction `0x94bcce…7aa19`](https://sepolia.etherscan.io/tx/0x94bcce51a24e7cf91ead0b0ff66e39780c60e601e26cb735e5b1ffb4f507aa19) confirmed in block `11721101`. Independent getters/events verify the configured reviewer, latest status/reason codes `1/1`, review count one, preserved history, four total expenses, and three review events. Refreshed metrics are ₹8,900 claims, ₹6,000 Attested, ₹900 Flagged, ₹2,000 Unreviewed, 89.0% claimed utilization, 77.5% reviewed coverage, and 67.4% attested coverage. Browser refresh showed Decision confirmed and the identical-repeat guard; production TypeScript/Vite build and lint pass.
- **Known limitations:** Receipt bytes for arbitrary submissions remain local and are not automatically available to the reviewer. Reviewer authorization proves control of the configured address, not organizational independence or truth of a decision. A mined receipt is the prototype finality threshold.
- **AI assistance:** AI implemented the reviewer workflow, role checks, transaction recovery, UI, tests, and documentation. The developer must inspect and explicitly approve any live reviewer transaction.

## 2026-09-17 — Stage 8: hardened static-host demonstration candidate

- **Changed:** Preserved confirmed expense/review hashes and explorer links when a subsequent public refresh fails; retained the last confirmed dataset with a nonblocking refresh warning; moved exhausted allocations off fresh claim forms while preserving the completed claim summary; and converted account/network changes during approval or pending confirmation into explicit failed/uncertain states without allowing callbacks from the prior wallet context to overwrite them. Added static-host-relative receipt URLs, hash-route-compatible Vite output, production metadata, stronger focus styling, a narrow-screen header, regression tests, and a GitHub Pages workflow that gates deployment on tests, lint, and build.
- **Hosting:** Selected GitHub Pages with prepared URL `https://vedbhoskar.github.io/TRACE/`. The build was served successfully from the simulated `/TRACE/` project path: the live Sepolia read, hash route, JS/CSS chunks, all four displayed receipt downloads, and explorer URLs resolved correctly. GitHub currently returns no Pages site for the repository, and this Stage 8 diff is intentionally uncommitted/unpushed, so the public URL is not live yet. The workflow requests Pages enablement after explicit approval and push.
- **Validation:** Thirteen test files and 86 tests pass. New boundaries cover refresh failure after confirmed expense/review receipts, pending-hash preservation on wallet revision, approval-time wallet changes, confirmed-state preservation, and exhausted-allocation selection. Production TypeScript/Vite build and lint pass with no warnings; `git diff --check` passes. Fresh live reads showed ₹8,900 claimed and the complete four-expense/three-review trail. Browser QA passed at 1470×835 projector/desktop and a 500×844 narrow viewport with no visible clipping at the tested widths; all four routes have one H1, header/nav/main/footer landmarks, no duplicate IDs, no unlabeled interactive controls, no desktop horizontal overflow, and a visible 3 px keyboard focus outline. Primary palette contrast ratios range from 4.80:1 to 15.74:1 on white. All five receipt assets and the contract/two live-acceptance Etherscan URLs returned HTTP 200.
- **Failure checks:** A wallet-free browser showed the explicit no-wallet state. A build pointed at an unreachable RPC showed only the live-read failure until the operator selected the labeled snapshot; snapshot mode displayed “not freshly checked,” kept both write routes disabled, and exposed no signer. Unknown donations remained distinct from RPC failure. Wrong account/network, rejected requests, delayed/uncertain confirmation, duplicate actions, and mid-flight account/network changes are covered by deterministic wallet/lifecycle tests; they were not re-enacted with additional live Sepolia writes. The previously completed manual Stage 6/7 sessions supply the confirmed submit → refresh → review → refresh evidence.
- **Known limitations:** No hosted-origin result exists until the prepared workflow is approved, committed, pushed, and GitHub Pages finishes deployment. Provider CORS/rate limits therefore remain to be confirmed from the final public origin. The narrowest visual run used 500 CSS pixels because macOS headless Chrome enforces a minimum window width; CSS breakpoints target 600 px and the application minimum is 320 px. No additional Sepolia write was sent. Arbitrary receipt bytes remain local, testnet role ownership is not organizational independence, and a mined receipt remains the prototype finality threshold.
- **AI assistance:** AI reviewed the live Stage 6/7 evidence, implemented transaction and refresh hardening, prepared static hosting, added tests, exercised controlled browser/RPC/snapshot states, inspected responsive and accessibility behavior, checked public links, and documented exact deployment limitations. Review the complete diff before using the suggested Stage 8 commit.
